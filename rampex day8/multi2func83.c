#include<stdio.h>
int mul(int a, int b)
{
    return a * b;
}

int main()
{
    printf("\n Multiplication of first two numbers: %d", mul(87, 67));
    printf("\n Multiplication of second two numbers: %d", mul(90, 20));
    return 0;
}
